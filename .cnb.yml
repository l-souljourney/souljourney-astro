# CNB 构建配置文件 - 精简生产版本
# SoulJourneyBlog Astro 构建部署

# 主分支：生产环境构建+部署
main:
  push:
    - imports: https://cnb.cool/l-souljourney/env/-/blob/main/env.yml
      env:
        TZ: Asia/Shanghai
      
      docker:
        image: node:18-alpine
        volumes:
          - node_modules:copy-on-write
          - /root/.local/share/pnpm:copy-on-write
          - /root/.cache/pnpm:copy-on-write
      stages:
        - name: build project
          script: |
            npm install -g pnpm@latest
            pnpm config set registry https://mirrors.cloud.tencent.com/npm/
            pnpm install --frozen-lockfile
            pnpm run build
            echo "✅ 构建完成: $(du -sh dist/ | cut -f1)"
            
        - name: deploy to github
          image: alpine/git:latest
          script: |
            if [ -z "${GITHUB_TOKEN}" ]; then
              echo "⚠️  跳过GitHub推送"
              exit 0
            fi
            
            git config --global user.name "CNB Auto Deploy"
            git config --global user.email "cnb@l-souljourney.cn"
            git remote set-url origin "https://${GITHUB_TOKEN}@github.com/l-souljourney/souljourney-astro.git"
            git push origin HEAD:main
            echo "✅ GitHub推送完成"
            
        - name: deploy to cos
          image: tencentcom/coscli:latest
          script: |
            echo "🔍 开始COS部署检查..."
            echo "📦 检查构建产物..."
            if [ ! -d "dist" ]; then
              echo "❌ 错误: dist目录不存在，构建可能失败"
              exit 1
            fi
            echo "✅ dist目录存在: $(du -sh dist/ | cut -f1)"
            
            echo "🔑 检查环境变量..."
            if [ -z "${COS_SECRET_ID}" ] || [ -z "${COS_SECRET_KEY}" ] || [ -z "${COS_BUCKET}" ] || [ -z "${COS_REGION}" ]; then
              echo "❌ COS环境变量不完整"
              echo "   COS_SECRET_ID: ${COS_SECRET_ID:+已设置}${COS_SECRET_ID:-未设置}"
              echo "   COS_SECRET_KEY: ${COS_SECRET_KEY:+已设置}${COS_SECRET_KEY:-未设置}"
              echo "   COS_BUCKET: ${COS_BUCKET:+已设置}${COS_BUCKET:-未设置}"
              echo "   COS_REGION: ${COS_REGION:+已设置}${COS_REGION:-未设置}"
              exit 1
            fi
            echo "✅ 环境变量检查通过"
            echo "   Bucket: ${COS_BUCKET}"
            echo "   Region: ${COS_REGION}"
            
            echo "⚙️  配置COS CLI..."
            coscli config set --secret_id "${COS_SECRET_ID}" --secret_key "${COS_SECRET_KEY}" || {
              echo "❌ COS CLI配置失败"
              exit 1
            }
            coscli config add --init-skip=true -b "${COS_BUCKET}" -r "${COS_REGION}" || {
              echo "❌ COS Bucket配置失败"
              exit 1
            }
            echo "✅ COS CLI配置完成"
            
            echo "🚀 开始同步文件到COS..."
            coscli sync dist/ "cos://${COS_BUCKET}/" --delete --force --disable-crc64 --recursive || {
              echo "❌ COS文件同步失败"
              exit 1
            }
            
            echo "✅ COS部署完成"
            if [ -n "${CDN_DOMAIN}" ]; then
              echo "🌐 访问地址: https://${CDN_DOMAIN}"
            fi

# Pull Request：快速检查
pull_request:
  push:
    - env:
        TZ: Asia/Shanghai
      
      docker:
        image: node:18-alpine
        volumes:
          - node_modules:copy-on-write
          - /root/.local/share/pnpm:copy-on-write
          - /root/.cache/pnpm:copy-on-write
      stages:
        - name: pr build check
          script: |
            npm install -g pnpm@latest
            pnpm config set registry https://mirrors.cloud.tencent.com/npm/
            pnpm install --frozen-lockfile
            pnpm run build
            echo "✅ PR构建检查通过"
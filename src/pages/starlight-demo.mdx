---
layout: "@/layouts/PageLayout/PageLayout.astro"
h1: "Starlight 设计示例"
desc: "展示 Starlight 设计体系的组件与样式"
type: "demo"
title: "Starlight 设计示例"
---

import StarlightAside from "@/components/Aside/StarlightAside.astro";

# Starlight 设计体系示例

本页面展示 v1.8.5 集成的 Starlight 设计元素。

## Aside 组件

Starlight 风格的提示框，支持 4 种类型。

<StarlightAside type="note" title="关于 Starlight 设计">
Starlight 是 Astro 官方的文档主题，提供了一套完整的设计规范。v1.8.5 提取其核心设计语言（字体、组件风格、动画规范），并融合到博客中。
</StarlightAside>

<StarlightAside type="tip">
💡 **小技巧**: 使用 Inter 字体搭配 JetBrains Mono，可以显著提升中英文混排和代码的阅读体验。
</StarlightAside>

<StarlightAside type="caution" title="兼容性提醒">
JetBrains Mono 的连字功能需要现代浏览器支持 OpenType 特性。如果你使用 IE 或旧版浏览器，可能无法看到连字效果。
</StarlightAside>

<StarlightAside type="danger">
⚠️ **重要警告**: v1.8.4 已删除所有自定义 CSS。如需恢复自定义样式，请使用 Git 回退到之前的版本。
</StarlightAside>

## 代码高亮主题

v1.8.5 采用 Starlight 推荐的 GitHub 主题族：

- **亮色模式**: `github-light`
- **暗色模式**: `github-dark-dimmed`

### JavaScript 示例

```javascript
// Starlight 字体配置
const fontConfig = {
  sans: ['Inter', 'system-ui', 'sans-serif'],
  mono: ['JetBrains Mono', 'Consolas', 'monospace'],
};

// OpenType 特性
const features = {
  cv02: 1, // Alternate g
  cv03: 1, // Alternate r  
  cv04: 1, // Alternate i
  cv11: 1, // Alternate single-story a
};

// 连字测试
if (a != b && c => d) {
  console.log('Test: !=, =>, >=, <=, ==');
}
```

### TypeScript 示例

```typescript
interface StarlightComponent {
  type: 'note' | 'tip' | 'caution' | 'danger';
  title?: string;
  children: React.ReactNode;
}

const AsideExample: React.FC<StarlightComponent> = ({ 
  type, 
  title, 
  children 
}) => {
  return (
    <aside className={`aside-${type}`}>
      {title && <h4>{title}</h4>}
      <div>{children}</div>
    </aside>
  );
};
```

### CSS 示例

```css
@layer base {
  body {
    font-family: 'Inter', -apple-system, sans-serif;
    font-feature-settings: 
      'cv02' 1, 
      'cv03' 1, 
      'cv04' 1, 
      'cv11' 1;
  }

  code {
    font-family: 'JetBrains Mono', monospace;
    font-variant-ligatures: common-ligatures;
  }
}
```

##  v1.8.5 改进汇总

| 特性 | v1.8.4 | v1.8.5 |
|------|--------|--------|
| 正文字体 | 系统默认 | **Inter** ✨ |
| 代码字体 | SF Mono | **JetBrains Mono** ✨ |
| OpenType | 无 | **cv02/cv03/cv04/cv11** ✨ |
| 代码连字 | 无 | **liga + calt** ✨ |
| 代码主题 | 默认 | **GitHub Light/Dark Dimmed** ✨ |
| Aside 组件 | 无 | **Starlight 风格** ✨ |

<StarlightAside type="tip" title="下一步">
Phase 3 将统一所有动画为 Starlight 规范（200ms + ease-out），并优化侧边栏交互效果。
</StarlightAside>
